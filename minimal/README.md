# Cmake template
Minimalistic cmake template.

- `src/` - contains main project files
- `src/project/*` - contains project libraries

```sh
cd build
cmake .. && make
cd src && ./my_project
```